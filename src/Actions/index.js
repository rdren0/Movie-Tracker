export const addMovies = (movies) =>({
  type:'ADD_MOVIES',
  movies
})

export const userLogin = (user) => ({
  type: 'USER_LOGIN', 
  user
})

export const addUser = (user) => ({
  type: 'ADD_USER',
  user
})
